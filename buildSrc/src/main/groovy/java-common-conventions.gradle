plugins {
    id 'java'
    id 'io.freefair.lombok'
}

group = 'com.maxed'
version = '0.0.1-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compileOnly libs.lombok
    annotationProcessor libs.lombok
}

tasks.named('test') {
    useJUnitPlatform()
}
