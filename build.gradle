defaultTasks 'clean', 'build'

subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    group = 'com.maxed'
    version = '0.0.1-SNAPSHOT'

    java {
        toolchain {
            languageVersion = JavaLanguageVersion.of(21)
        }
    }
}
